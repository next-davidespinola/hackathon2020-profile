version: '3.3'

services:
  postgres:
    image: postgres:9.6-alpine
    ports:
      - 5432:5432

    environment:
      POSTGRES_DB: kids-db
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
